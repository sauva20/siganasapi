# ============================================================
# DEPENDENCIES - NANAS GRADING BACKEND
# Install: pip install -r requirements.txt
# ============================================================

# --- Web Framework ---
fastapi>=0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9       # Untuk upload file (Form & File)
gradio==4.44.0
huggingface-hub==0.22.2       # KUNCI JAWABAN FIX ERROR HfFolder UNTUK ZEROGPU



# --- Database ---
sqlalchemy>=2.0.30
pymysql==1.1.0                # MySQL driver untuk SQLAlchemy
cryptography==42.0.7          # Dependensi pymysql untuk koneksi aman

# --- Validasi & Settings ---
pydantic>=2.7.1
pydantic-settings==2.2.1      # Untuk class Settings dari .env

# --- Keamanan ---
passlib[bcrypt]==1.7.4        # Hashing password bcrypt
bcrypt==4.0.1                 # PENTING: dipin ke 4.0.1 — passlib 1.7.4 error
                               # ("module 'bcrypt' has no attribute '__about__'")
                               # jika terpasang bcrypt>=4.1.0. Jangan upgrade
                               # tanpa upgrade passlib juga.
python-jose[cryptography]==3.3.0  # JWT token

# --- Traceability ---
qrcode[pil]==7.4.2            # Generate QR code image (PNG) untuk kemasan batch

# --- AI / Computer Vision ---
ultralytics==8.2.18           # YOLOv11 (via ultralytics)
Pillow>=10.3.0                # Image processing

# --- Utilitas ---
python-dotenv==1.0.1          # Load file .env
aiofiles==23.2.1              # Async file I/O untuk upload

# --- Development (opsional, hapus di production) ---
httpx==0.27.0                 # Untuk testing async client
pytest==8.2.0
pytest-asyncio==0.23.6
